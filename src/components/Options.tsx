import React from 'react';
import ToggleButton from './ToggleButton';


const Options = () => {
  return (
    <div>
      <ToggleButton />
      <span>완료 항목</span>
    </div>
  );
};

export default Options;
